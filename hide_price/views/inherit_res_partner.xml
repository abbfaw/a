<odoo>
    <data>
        <!-- Ajout d'un filtre pour trier les inscrits depuis le site web -->
        <record id="view_res_partner_filter_inherit" model="ir.ui.view">
            <field name="name">res.partner.view.search</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_res_partner_filter"/>
            <field name="arch" type="xml">
                <field name="user_id" position="after">
                    <field name="from_web"/>
                </field>
                <xpath expr="//filter[@name='inactive']" position="after">
                    <filter string="Visitors" name="visitor_partner_id"
                            domain="[('partner_share', '=', True), ('category_id.color', '=', 9)]"/>
                </xpath>
            </field>
        </record>

        <!-- retrait de optional hide sur le champ category_id dans les contacts -->
        <record id="view_partner_tree_inherit" model="ir.ui.view">
            <field name="name">res.partner.view.tree.inherit</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_tree"/>
            <field name="arch" type="xml">
                <field name="category_id" position="attributes">
                    <attribute name="optional">show</attribute>
                </field>
            </field>
        </record>

        <!-- Vue Liste de la vente -->
        <record id="view_quotation_tree_modded" model="ir.ui.view">
            <field name="name">sale.order.tree</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_quotation_tree"/>
            <field name="arch" type="xml">
                <field name="tag_ids" position="attributes">
                    <attribute name="optional">show</attribute>
                </field>
            </field>
        </record>
    </data>
</odoo>